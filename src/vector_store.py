# ChromaDB wrapper for kNN risk features, UI similarity, and training-data dedup.
# This is intentionally NOT a RAG retriever - we never feed retrieved text back into LLM prompts.
# Filled in at STEP 5.

# Preferred LLM provider for the demo path (src/agent.py::_llm_hf_api).
# Free tier at console.groq.com — ~30 req/min, serves Llama-3.3-70B.
GROQ_API_KEY=

# Only used by the fallback branch if GROQ_API_KEY is unset, and by the
# notebooks that pull Phi-3-mini for fine-tuning.
HF_TOKEN=
